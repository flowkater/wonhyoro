.event
	.navbar
		.navbar-inner
			.brand
				i.icon-star
			= link_to "이벤트관리 >", events_path, class: "brand"
			= link_to "#{@event.title}", event_path(@event), class: "brand"

	h4 이벤트 내용
	span = @event.content

	h4 티저(teaser)
	.teaser-loc
		.plus-btn = link_to new_event_teaser_path(@event), class: "btn btn-info new-teaser" do
			.plus +
		= render 'teasers/teasers'
	
		

	h4 포스터(picture)
	= render 'pictures/form'
	.poster-loc
		= render 'pictures/pictures'

	
